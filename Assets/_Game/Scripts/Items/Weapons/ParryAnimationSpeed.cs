namespace ZZ
{
    /// <summary>Identifies the authored startup and window timing of a Parry.</summary>
    public enum ParryAnimationSpeed : byte
    {
        Fast = 0,
        Medium = 1,
        Slow = 2
    }
}
