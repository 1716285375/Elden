namespace ZZ
{
    /// <summary>Defines the non-combat behavior performed while an AI remains idle.</summary>
    public enum IdleStateMode : byte
    {
        Idle = 0,
        Patrol = 1,
        Sleep = 2
    }
}
